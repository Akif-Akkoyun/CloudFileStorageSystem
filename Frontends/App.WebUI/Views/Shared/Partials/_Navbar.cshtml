<header>
    <nav class="navbar navbar-expand-lg navbar-dark ftco_navbar ftco-navbar-light site-navbar-target" id="ftco-navbar">
        <div class="container">
            <a class="navbar-brand" asp-action="Index" asp-controller="Home">Cloud</a>
            <button class="navbar-toggler js-fh5co-nav-toggle fh5co-nav-toggle" type="button" data-toggle="collapse" data-target="#ftco-nav" aria-controls="ftco-nav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="oi oi-menu"></span> Menu
            </button>

            <div class="collapse navbar-collapse" id="ftco-nav">
                <ul class="navbar-nav nav mr-auto">
                    <li class="nav-item">
                        <a asp-action="Index" asp-controller="Home" class="nav-link"><span>Ana Sayfa</span></a>
                        @if (User.Identity.IsAuthenticated)
                        {
                        <li class="nav-item"><a asp-action="MyFiles" asp-controller="MyPage" class="nav-link"><span>Dosyalarım</span></a></li>
                        <li class="nav-item"><a asp-action="SharedWithMe" asp-controller="MySharedPage" class="nav-link"><span>Benimle Paylaşılan</span></a></li>
                        <li class="nav-item"><a asp-action="SharedByMe" asp-controller="MySharedPage" class="nav-link"><span>Benim Paylaştığım</span></a></li>
                        }                        
                    </li>
                </ul>
                <ul class="navbar-nav nav ml-auto">
                    @if (User.Identity.IsAuthenticated)
                    {
                        <li class="nav-item"><a asp-action="Logout" asp-controller="Auth" class="nav-link"><span>Logout</span></a></li>
                    }
                    else
                    {
                        <li class="nav-item"><a asp-action="Login" asp-controller="Auth" class="nav-link"><span>Login/Register</span></a></li>
                    }
                </ul>
            </div>
        </div>
    </nav>
</header>
